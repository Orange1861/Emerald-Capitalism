# 26.2 ships Mojang's real parameter/variable names directly (obfuscation removed
# starting with 26.1 snapshots), so no Parchment mappings block is needed here.

# Environment Properties
# You can find the latest versions here: https://projects.neoforged.net/neoforged/neoforge
# The Minecraft version must agree with the Neo version to get a valid artifact
minecraft_version=26.2
java_version=25
minecraft_version_range=[26.2]
# The Neo version must agree with the Minecraft version to get a valid artifact
# Current as of NeoForge's maven metadata, Aug 29 2026 (this is also the
# explicit <latest>/<release> value in their metadata, not just the newest in the list).
neo_version=26.2.0.72
# The loader version range can only use the major version of FML as bounds
loader_version_range=[1,)
